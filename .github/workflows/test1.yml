name: test1

on:
  workflow_dispatch:
    inputs:
      test:
        description: 'Test'
        required: false
        type: boolean
  pull_request:
    branches: [ main, master ]

jobs:
  hello_world:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Hello World!"
  # raise_error:
  #   runs-on: ubuntu-latest
  #   steps:
  #     - run: exit 1


